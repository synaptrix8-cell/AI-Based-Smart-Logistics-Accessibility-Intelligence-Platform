# Tools

Deterministic Python scripts that handle actual execution — API calls, data transformations, file operations, database queries.

## Conventions

- Each tool is a standalone `.py` script
- Credentials are loaded from `../.env` (never hardcoded)
- Scripts should be consistent, testable, and fast
- Check here first before building anything new
